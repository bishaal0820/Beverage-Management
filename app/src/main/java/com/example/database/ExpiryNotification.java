package com.example.database;

import android.widget.Toast;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Date;

public class ExpiryNotification {

    //Calculate the difference between current time and date

/*   SimpleDateFormat sdf1 = new SimpleDateFormat("dd-MM-yyyy");
    String dateis=edittext.getText().toString();


    Date dates = null;
                try {
        dates = sdf1.parse(sText);
    } catch (
    ParseException e) {
        e.printStackTrace();
    }

    Date curr = now.getTime();
    long diff = dates.getTime() - curr.getTime();
    int diffDays = (int) (diff / (24 * 60 * 60 * 1000))+1;
                Toast.makeText(MainActivity.this,"Date "+ diffDays , Toast.LENGTH_SHORT).show();*/
}
